server:
  port: 8402

spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/iat_ai_phm
    username: postgres
    password: 1004
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true